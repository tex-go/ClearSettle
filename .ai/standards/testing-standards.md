# Testing Standards

Targets
- Backend: target >=85% coverage for critical modules.
- Frontend: target >=80% for UI modules under change.

Types
- Unit tests, integration tests, end-to-end smoke tests and fuzz tests for parsers.

CI
- Tests run in CI pipelines with coverage gating on PRs for changed modules.
