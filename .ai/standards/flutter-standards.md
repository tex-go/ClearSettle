# Flutter Standards

Guidelines for future mobile client.

Key points
- Offline-first: local persistence with `Hive` and sync queues.
- Use `Riverpod` for state and dependency injection.
- Background tasks should be resilient and idempotent.

Testing
- Unit and widget tests; integration tests for sync flows.
