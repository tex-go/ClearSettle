"""
Analytics service.

Entry points:
  queries.get_dashboard_summary(db, company_id, ...)
  queries.get_revenue_trends(db, company_id, ...)
  queries.get_fee_breakdown(db, company_id, ...)
  queries.get_payout_trends(db, company_id, ...)
  queries.get_refund_trends(db, company_id, ...)
  queries.get_marketplace_breakdown(db, company_id, ...)
  queries.get_reconciliation_health(db, company_id, ...)
  queries.get_profitability_metrics(db, company_id, ...)
"""
